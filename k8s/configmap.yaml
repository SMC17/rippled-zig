apiVersion: v1
kind: ConfigMap
metadata:
  name: rippled-zig-config
  labels:
    app.kubernetes.io/name: rippled-zig
    app.kubernetes.io/component: config
data:
  RPC_PORT: "5005"
  PEER_PORT: "51235"
  WS_PORT: "6006"
  LOG_LEVEL: "info"
  DATA_DIR: "/var/lib/rippled"
