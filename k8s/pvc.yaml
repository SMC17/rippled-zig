apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: rippled-zig-data
  labels:
    app.kubernetes.io/name: rippled-zig
    app.kubernetes.io/component: storage
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
