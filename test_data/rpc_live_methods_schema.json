{
  "schema_version": 1,
  "methods": {
    "server_info": {
      "required_fields": ["result"],
      "required_result_fields": ["info", "status"],
      "required_info_fields": ["build_version", "complete_ledgers", "server_state", "validated_ledger"],
      "required_validated_ledger_fields": ["seq", "hash"],
      "expected_status": "success"
    },
    "fee": {
      "required_fields": ["result"],
      "required_result_fields": ["drops", "ledger_current_index", "status"],
      "required_drops_fields": ["base_fee", "minimum_fee", "median_fee", "open_ledger_fee"],
      "expected_status": "success"
    },
    "account_info": {
      "required_fields": ["result"],
      "required_result_fields": ["account_data", "ledger_current_index", "status", "validated"],
      "required_account_data_fields": ["Account", "Balance", "Flags", "OwnerCount", "Sequence"],
      "expected_status": "success",
      "expected_validated": true
    },
    "submit": {
      "required_fields": ["result"],
      "required_result_fields": ["engine_result", "engine_result_code", "status", "validated", "tx_json"],
      "required_tx_json_fields": ["TransactionType", "Fee", "Sequence", "Destination", "Amount"],
      "expected_status": "success",
      "expected_engine_result": "tesSUCCESS",
      "expected_tx_type": "payment"
    },
    "ping": {
      "required_fields": ["result"]
    },
    "ledger_current": {
      "required_fields": ["result"],
      "required_result_fields": ["ledger_current_index"]
    }
  }
}
